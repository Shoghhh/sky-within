# API host when using a physical device (iPhone/Android phone).
# Leave unset for simulator (localhost) or Android emulator (10.0.2.2).
# Find your Mac's IP: System Settings → Network → Wi-Fi → Details
# Example: EXPO_PUBLIC_API_HOST=192.168.1.5
# EXPO_PUBLIC_API_HOST=
